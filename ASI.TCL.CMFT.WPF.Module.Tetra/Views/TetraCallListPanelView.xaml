<UserControl
    x:Class="ASI.TCL.CMFT.WPF.Module.Tetra.Views.TetraCallListPanelView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:applications="clr-namespace:ASI.TCL.CMFT.WPF.Applications;assembly=ASI.TCL.CMFT.WPF"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:converters="clr-namespace:ASI.TCL.CMFT.WPF.Module.Tetra.Converters"
    xmlns:converters1="clr-namespace:ASI.TCL.CMFT.WPF.Converters;assembly=ASI.TCL.CMFT.WPF"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dataTypes="clr-namespace:ASI.TCL.CMFT.WPF.Module.Tetra.DataTypes"
    xmlns:datas="clr-namespace:ASI.TCL.CMFT.WPF.Module.Tetra.Datas"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:system="clr-namespace:System;assembly=mscorlib"
    xmlns:triggerActions="clr-namespace:ASI.TCL.CMFT.WPF.TriggerActions;assembly=ASI.TCL.CMFT.WPF"
    xmlns:viewModels="clr-namespace:ASI.TCL.CMFT.WPF.Module.Tetra.ViewModels"
    d:DataContext="{d:DesignInstance viewModels:TetraCallListPanelViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="600"
    d:DesignWidth="300"
    Style="{StaticResource CMFT.UserControl}"
    mc:Ignorable="d">

    <!--  無線電來電  -->
    <GroupBox Header="無線電來電 等候佇列" Style="{x:Null}">
        <StackPanel>
            <Button
                Margin="5"
                Command="{Binding AnswerTetraCallCommand}"
                Content="接聽"
                Style="{DynamicResource CMFT.Button}" />
            <ListView
                Height="180"
                ItemsSource="{Binding TetraCallQueue}"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                SelectedItem="{Binding SelectedTetraItem}">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Width="100" Header="號碼">
                            <GridViewColumn.HeaderContainerStyle>
                                <Style BasedOn="{StaticResource GridView.ColumnHeader.Center}" TargetType="GridViewColumnHeader">
                                    <Setter Property="FontSize" Value="14" />
                                    <Setter Property="Height" Value="30" />
                                </Style>
                            </GridViewColumn.HeaderContainerStyle>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Foreground="Black"
                                        Text="{Binding PhoneNumber}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="100" Header="基地台">
                            <GridViewColumn.HeaderContainerStyle>
                                <Style BasedOn="{StaticResource GridView.ColumnHeader.Center}" TargetType="GridViewColumnHeader">
                                    <Setter Property="FontSize" Value="14" />
                                </Style>
                            </GridViewColumn.HeaderContainerStyle>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Foreground="Black"
                                        Text="{Binding BaseStation}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
                <ListView.ItemContainerStyle>
                    <Style BasedOn="{StaticResource ListView.GridViewItem.Stretch}" TargetType="ListViewItem">
                        <Setter Property="FontSize" Value="12" />
                        <Setter Property="Height" Value="30" />
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="LightBlue" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Background" Value="LightGreen" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </StackPanel>
    </GroupBox>
</UserControl>
