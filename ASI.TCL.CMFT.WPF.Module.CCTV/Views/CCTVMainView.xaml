<UserControl x:Class="ASI.TCL.CMFT.WPF.Module.CCTV.Views.CCTVMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters1="clr-namespace:ASI.TCL.CMFT.WPF.Converters;assembly=ASI.TCL.CMFT.WPF"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Style="{StaticResource CMFT.UserControl}"
             mc:Ignorable="d">

    <Grid>


        <Grid.Resources>
            <Style x:Key="RaisedToggleButton"
                   BasedOn="{StaticResource MaterialDesignFlatMidBgButton}"
                   TargetType="{x:Type ToggleButton}">
                <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="4" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="{DynamicResource CMFT.Brush.Gray.Dark}" />
                <Setter Property="Foreground" Value="{DynamicResource AssitColor1}" />
                <Setter Property="Background" Value="{Binding BorderBrush, RelativeSource={RelativeSource Self}}" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Opacity" Value="0.8" />
                    </Trigger>
                    <Trigger Property="IsChecked" Value="True">
                        <Setter Property="Opacity" Value="0.5" />
                        <Setter Property="FontWeight" Value="Bold" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>





        <ToggleButton Margin="10"
                      HorizontalAlignment="Right"
                      VerticalAlignment="Top"
                      IsChecked="{Binding IsChecked, ElementName=PowerToggleButton}" />
        <ToggleButton x:Name="PowerToggleButton"
                      Width="350"
                      Height="200"
                      Padding="48,8"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      FontSize="28"
                      IsEnabled="{Binding IsChecked, RelativeSource={RelativeSource Self}, Converter={converters1:BoolNegationConverter}}"
                      Style="{StaticResource RaisedToggleButton}">



            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <materialDesign:PackIcon Width="150"
                                         Height="150"
                                         HorizontalAlignment="Center"
                                         VerticalAlignment="Center"
                                         Kind="Power" />
                <TextBlock x:Name="PARTText"
                           Margin="0,-10,0,0"
                           HorizontalAlignment="Stretch"
                           FontSize="{Binding FontSize, ElementName=PowerToggleButton}"
                           FontWeight="Bold"
                           TextAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value=" 開啟 " />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=PowerToggleButton, Mode=TwoWay}" Value="True">
                                    <Setter Property="Text" Value="已開啟" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>



                </TextBlock>
            </StackPanel>
        </ToggleButton>
    </Grid>


</UserControl>
