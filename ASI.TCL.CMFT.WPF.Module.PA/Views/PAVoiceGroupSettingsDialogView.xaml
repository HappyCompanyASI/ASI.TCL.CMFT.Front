<UserControl
    x:Class="ASI.TCL.CMFT.WPF.Module.PA.Views.PAVoiceGroupSettingsDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:enumType="clr-namespace:ASI.TCL.CMFT.WPF.Dialogs;assembly=ASI.TCL.CMFT.WPF"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="clr-namespace:ASI.TCL.CMFT.WPF.Module.PA.ViewModels"
    Width="300"
    d:DataContext="{d:DesignInstance viewModels:PAVoiceGroupSettingsDialogViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="200"
    d:DesignWidth="300"
    Style="{StaticResource CMFT.UserControl}"
    mc:Ignorable="d">
    <UserControl.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding OKCommand}" />
        <KeyBinding Key="Esc" Command="{Binding CancelCommand}" />
    </UserControl.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ContentControl
            Grid.Row="0"
            Margin="0,0,0,10"
            Content="請輸入群組名稱"
            Style="{StaticResource DialogHeader}" />

        <StackPanel Grid.Row="1" Margin="20">
            <TextBox Text="{Binding GroupName, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Margin="0,10,0,0" Text="{Binding HintText}" />
        </StackPanel>

        <ContentControl
            Grid.Row="3"
            Content="{x:Static enumType:eDialogFooterButtons.OKCancel}"
            Style="{StaticResource DialogFooter}" />
    </Grid>
</UserControl>
