<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>

        <ResourceDictionary Source="pack://application:,,,/ASI.TCL.CMFT.WPF;component/Themes/Panel.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style
        x:Key="CMFT.GridView.ColumnHeader.Left"
        BasedOn="{StaticResource MaterialDesignToolButton}"
        TargetType="{x:Type GridViewColumnHeader}">
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="FontSize" Value="{DynamicResource DataGridHeaderSize}" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="TextBlock.Foreground" Value="{DynamicResource MaterialDesign.Brush.Foreground}" />
    </Style>

    <Style
        x:Key="CMFT.GridView.ColumnHeader.Center"
        BasedOn="{StaticResource CMFT.GridView.ColumnHeader.Left}"
        TargetType="{x:Type GridViewColumnHeader}">
        <Setter Property="HorizontalContentAlignment" Value="Center" />
    </Style>

</ResourceDictionary>